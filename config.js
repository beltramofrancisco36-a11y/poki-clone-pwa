// Opciones y placeholders. Si quieres usar RAWG u otra API, pon la key aquí.
const CONFIG = {
  RAWG_API_KEY: null, // coloca tu API key si quieres obtener juegos reales
  RAWG_ENDPOINT: 'https://api.rawg.io/api/games',
  XP_PER_PLAY: 10,
  GAME_SECONDS: 3
};
